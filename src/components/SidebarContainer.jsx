import ProfileContainer from "./ProfileContainer"
import styles from "./sass_components/SidebarContainer.module.scss"

const SidebarContainer = () => {
  return (
    <aside className={styles.sidebar}>
      
    </aside>
    )
}

export default SidebarContainer